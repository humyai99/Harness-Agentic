"""The HTTP API and the browser UI over it.

A subscriber to the same event stream the terminal renders, never a second code
path -- which is why :mod:`harness_agentic.core.events` exists in the first
milestone rather than this one.
"""
