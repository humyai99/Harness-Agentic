"""Scheduled, unattended runs.

The surface where a mistake is most expensive and least visible, so the defaults
here are the strictest in the framework -- approval is DENY, overlap is refused,
and a missed window is not backfilled.
"""
