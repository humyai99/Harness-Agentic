"""Third-party extensions, loaded from three sources.

A plugin is somebody else's code running in this process, so what it registers
is constrained after the fact rather than trusted: see
:mod:`harness_agentic.plugins.loader`.
"""
