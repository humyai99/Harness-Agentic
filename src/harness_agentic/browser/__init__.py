"""Browser automation: the page model and the driver behind it.

The page model is the interesting half. An agent reads a page as an accessibility
outline with element references, not as pixels -- see
:mod:`harness_agentic.browser.page` for why.
"""
