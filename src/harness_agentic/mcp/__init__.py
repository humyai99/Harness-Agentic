"""An MCP client: protocol, stdio transport, and the bridge onto the registry.

An MCP server is somebody else's code, so the trust rules are in
:mod:`harness_agentic.mcp.bridge` and they only ever tighten what a server
declares about itself.
"""
